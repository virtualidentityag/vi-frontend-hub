{
  "scripts": {
    "start": "docz dev",
    "build": "docz build",
    "netlify": "yarn build && cp netlify.toml ./dist/netlify.toml && cp -R src/assets ./dist"
  },
  "devDependencies": {
    "docz": "^0.5.8"
  },
  "dependencies": {
    "styled-components": "^3.3.3"
  }
}
